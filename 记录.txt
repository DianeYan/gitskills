第一阶段：准备阶段(2017.03.22-2017.03.24)1.熟练使用vim、svn总结：   vim 1.php    q    退出    :wq  保存退出    :q   退出，会提示是否保存    :q!  强制退出，不保存    i  I  插入    ESC  退出插入模式   Tab不全   svn:简写与常用 http://www.cnblogs.com/likwo/archive/2010/09/08/1821232.html    svn checkout svn://192.168.1.1/pro/domain   简写：svn co    svn add test.php(添加test.php)   svn add *.php(添加当前目录下所有的php文件)     svn commit -m “add test file for my test“ test.php   简写：svn ci    svn lock -m “lock test file“ test.php    svn unlock PATH     svn update如果后面没有目录，默认将当前目录以及子目录下的所有文件都更新到最新版本。    svn update -r 200 test.php(将版本库中的文件test.php还原到版本200)    svn update test.php(更新，于版本库同步。如果在提交的时候提示过期的话，是因为冲突，需要先update，修改文件，然后清除svn resolved，最后再提交commit)   简写：svn up   1）svn status path（目录下的文件和子目录的状态，正常状态不显示）  【?：不在svn的控制中；M：内容被修改；C：发生冲突；A：预定加入到版本库；K：被锁定】  2）svn status -v path(显示文件和子目录状态)  第一列保持相同，第二列显示工作版本号，第三和第四列显示最后一次修改的版本号和修改人。  注：svn status、svn diff和 svn revert这三条命令在没有网络的情况下也可以执行的，原因是svn在本地的.svn中保留了本地版本的原始拷贝。  简写：svn st   svn delete path -m “delete test fle“  例如：svn delete svn://192.168.1.1/pro/domain/test.php -m “delete test file”  或者直接svn delete test.php 然后再svn ci -m ‘delete test file‘，推荐使用这种  简写：svn (del, remove, rm)  svn diff path(将修改的文件与基础版本比较)  例如：svn diff test.php  svn diff -r m:n path(对版本m和版本n比较差异)  例如：svn diff -r 200:201 test.php  简写：svn di   svn merge -r m:n path  例如：svn merge -r 200:205 test.php（将版本200与205之间的差异合并到当前文件，但是一般都会产生冲突，需要处理一下）配置codereview总结：   *未配置搭建nginx、php-fpm服务器环境（phpstudy）,理解http code(404,403,400,200,502,503,500等)总结：   200 正常，请求已完成   400 请求无效/错误请求，请求有语法错误/不能满足请求   403 禁止访问，各种禁止原因 http://www.cnblogs.com/kunhony/archive/2006/06/16/427305.html   404 找不到页面/站点   500 服务器内部错误，一般为代码语法错误   502 错误网关，   503 无法获取服务，维护或过载   了解RSA非对称加密算法（1天）总结： *openssl_pkey_get_private 获取私钥 openssl_pkey_get_private 获取公钥     openssl_private_encrypt 私钥加密  openssl_public_encrypt 公钥加密  return (openssl_private_decrypt(base64_decode($encrypted), $decrypted, self::getPrivateKey())) ? $decrypted : null; 私钥解密  return (openssl_public_decrypt(base64_decode($encrypted), $decrypted, self::getPublicKey())) ? $decrypted : null; 公钥解密   ******不熟悉融360的代码开发，测试上线流程和svn开发流程 2.学习Yii，理清代码结构，和代码、配置、日志在服务器的对应位置3.学习rong360基础类: rongcrypt_decrypt、rongcrypt_encrypt、RongCache、DBModel、ApiClient、ApiServer、RiskYunStorage等(见wiki)（1天）目标：适应linux下开发，使用vim、svn，熟悉Yii和公司类库.（1天）
